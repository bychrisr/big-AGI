# Story 3.3: Criar rota /api/minds

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [api_contract_validation, auth_test, response_format_check]

## Story
**As a** usuario autenticado do teamAI,
**I want** acessar uma lista dos minds disponiveis para mim via API,
**so that** o frontend possa popular personas e opcoes de debate com os minds corretos.

## Acceptance Criteria
1. GET /api/minds retorna lista de minds com metadata (id, name, specialty, tags, token_budget)
2. Autenticacao Supabase JWT obrigatoria (401 se nao autenticado)
3. Retorna minds compartilhados (`squads-base/mmos-squad/minds/`) + personalizados do usuario (`users/{user}/minds/`)
4. Formato compativel com sistema de Personas do big-AGI (`SimplePersona` interface)

## Tasks / Subtasks
- [x] Task 1: Definir schema da resposta (AC: 1, 4)
  - [x] 1.1 Definir tipo `MindMetadata`: `{ id: string, name: string, specialty: string, tags: string[], tokenBudget: number, source: 'shared' | 'custom', systemPromptPreview?: string }`
  - [x] 1.2 Definir tipo de resposta: `{ minds: MindMetadata[] }`
  - [x] 1.3 Mapear `MindMetadata` para `SimplePersona` do big-AGI: `id` → `id`, `name` → `name`, `systemPrompt` lido de `system_prompt.md`, `creationDate` de `metadata.yaml`

- [x] Task 2: Implementar leitor de minds do filesystem (AC: 3)
  - [x] 2.1 Criar `src/server/minds/minds.reader.ts`
  - [x] 2.2 Funcao `readSharedMinds()`: le `squads-base/mmos-squad/minds/*/metadata.yaml` do repo teamai
  - [x] 2.3 Funcao `readUserMinds(userId)`: le `users/{userId}/minds/*/metadata.yaml` do repo teamai
  - [x] 2.4 Funcao `mergeMinds(shared, custom)`: combina e desduplicca (custom override shared se mesmo id)
  - [x] 2.5 Cache em memoria com TTL de 5 minutos (evitar I/O a cada request)
  - [x] 2.6 Configurar path base do repo teamai via env var `TEAMAI_REPO_PATH`

- [x] Task 3: Criar route handler (AC: 1, 2)
  - [x] 3.1 Criar `app/api/minds/route.ts` como Route Handler do App Router
  - [x] 3.2 Extrair e validar JWT Supabase do request (stub — Story 3.1 fara validacao completa)
  - [x] 3.3 Identificar user_id a partir do JWT
  - [x] 3.4 Resolver username do user_id (mapeamento estatico por ora)
  - [x] 3.5 Chamar `readSharedMinds()` + `readUserMinds(username)`
  - [x] 3.6 Retornar JSON com lista combinada
  - [x] 3.7 Error handling: 401 (nao autenticado), 500 (erro leitura filesystem)

- [x] Task 4: Parsing de metadata.yaml (AC: 1)
  - [x] 4.1 Parser YAML inline (sem dependencia extra — yaml e js-yaml sao transitive deps nao declaradas)
  - [ ] 4.2 Schema de validacao com Zod para `metadata.yaml` (parsing basico com warns por ora)
  - [x] 4.3 Tratar `metadata.yaml` ausente ou malformado: logar warning, pular mind
  - [x] 4.4 Ler `system_prompts/main.md` do diretorio do mind para incluir preview no response

- [x] Task 5: Mapear para formato SimplePersona (AC: 4)
  - [x] 5.1 Criar funcao `mindToSimplePersona(mind: MindMetadata, systemPrompt: string): SimplePersona`
  - [x] 5.2 Mapeamento: `id` → `id`, `name` → `name`, `systemPrompt` → conteudo de `system_prompt.md`, `creationDate` → data do `metadata.yaml` ou data fixa
  - [x] 5.3 Adicionar endpoint alternativo ou flag para retornar no formato SimplePersona diretamente
  - [x] 5.4 Incluir campo `pictureUrl` se existir imagem no diretorio do mind

## Dev Notes

### Arquivos relacionados no big-AGI
- `src/apps/personas/store-app-personas.ts` — tipo `SimplePersona` com campos: `id`, `name`, `systemPrompt`, `creationDate`, `pictureUrl`, `inputProvenance`, `inputText`, `llmLabel`
- `app/api/` — diretorio de Route Handlers do App Router
- `src/server/env.server.ts` — onde adicionar `TEAMAI_REPO_PATH`

### Padroes existentes a seguir
- Route Handlers: `app/api/*/route.ts` com export de `GET`, `POST`, etc.
- Env vars: `createEnv()` com Zod schema em `env.server.ts`
- Response format: JSON com tipos TypeScript compartilhados

### APIs e tipos relevantes
- `SimplePersona` de `src/apps/personas/store-app-personas.ts`: interface principal de personas
- `metadata.yaml` schema (definido no repo teamai): `{ id, name, specialty, token_budget, tags }`
- Supabase Auth: `getUser()` do server client para extrair user info

### Estrutura de um mind no repo teamai
```
squads-base/mmos-squad/minds/alex_hormozi/
  metadata.yaml        # { id, name, specialty, token_budget, tags }
  system_prompt.md     # System prompt completo
  kb/                  # Knowledge base chunks (nao necessario para esta story)
```

### Testing
- Framework: vitest
- Localizacao dos testes: colocados ao lado do arquivo
- O que testar: parsing de metadata.yaml, merge de minds shared+custom, auth rejection, resposta JSON valida
- Mock: filesystem (para nao depender do repo teamai em CI)
- Testar mind com metadata invalido e pular sem crash

## File List
- `src/server/minds/minds.reader.ts` — leitor de minds do filesystem com cache
- `src/server/minds/minds.reader.test.ts` — testes vitest (requer vitest instalado)
- `app/api/minds/route.ts` — GET /api/minds route handler
- `.env.example` — adicionado TEAMAI_REPO_PATH

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-26 | 1.0 | Story criada | Morgan (PM) |
| 2026-02-26 | 1.1 | Tasks 1-4 implementadas; Task 5 (SimplePersona mapping) pendente | Dev Agent |
| 2026-02-26 | 1.2 | Task 5 implementada: mindToSimplePersona, ?format=persona, pictureUrl | Dev Agent |
