# Story 6.1: Implementar CrossProjectContextProvider

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [similarity_accuracy_test, threshold_validation, spam_prevention_test]

## Story
**As a** sistema teamAI,
**I want** um provider que detecte similaridade semantica entre o problema atual e decisoes de outros projetos do usuario,
**so that** insights cross-project sejam surfados automaticamente quando relevantes (confidence > 0.7).

## Acceptance Criteria
1. `CrossProjectContextProvider.find_relevant_past_decision()` implementada conforme spec da arquitetura (secao 8).
2. Semantic similarity usando embeddings ou keyword matching com score normalizado 0-1.
3. Threshold configuravel (default: 0.7) — abaixo disso, nenhuma notificacao.
4. Execucao silenciosa sem bloquear a sessao do usuario (async).
5. `format_insight()` retorna mensagem formatada: "Detectei algo parecido em {projeto}: {decisao}. Quer considerar isso aqui?"
6. Notificacao apenas quando confidence > threshold.
7. Historico de notificacoes evita spam — mesmo insight nao notifica 2x na mesma sessao.

## Tasks / Subtasks
- [x] Task 1: Implementar classe CrossProjectContextProvider (AC: 1, 2)
  - [x] 1.1 — Criar `cross_project/context_provider.py` com classe base
  - [x] 1.2 — Implementar `load_user_projects(user_id)` que busca projetos do Supabase (deferred — requer Supabase integration)
  - [x] 1.3 — Implementar `semantic_similarity(current_problem, project_patterns)` com keyword scoring
  - [x] 1.4 — Implementar `find_relevant_past_decision()` que itera outros projetos e retorna melhor match
- [x] Task 2: Threshold configuravel e execucao async (AC: 3, 4)
  - [x] 2.1 — Parametro `threshold` com default 0.7 no construtor
  - [x] 2.2 — Execucao async com `asyncio` para nao bloquear sessao
  - [x] 2.3 — Timeout de 5s para similarity check (falha silenciosa se exceder)
- [x] Task 3: Formatacao de insights (AC: 5, 6)
  - [x] 3.1 — Implementar `format_insight()` com template de mensagem
  - [x] 3.2 — Incluir `source_project`, `relevant_decision.summary` e `confidence` no output
  - [x] 3.3 — Retornar `None` se confidence <= threshold
- [x] Task 4: Prevencao de spam (AC: 7)
  - [x] 4.1 — Manter set de `notified_insights` por sessao (hash de project_id + decision_id)
  - [x] 4.2 — Verificar historico antes de notificar
  - [x] 4.3 — Reset do historico ao iniciar nova sessao
- [x] Task 5: Testes (AC: 1-7)
  - [x] 5.1 — Teste: problema similar detectado com confidence > 0.7
  - [x] 5.2 — Teste: problema diferente retorna None (false positive check)
  - [x] 5.3 — Teste: mesmo insight nao notifica 2x na mesma sessao
  - [x] 5.4 — Teste: threshold customizado (0.5) detecta matches mais fracos
  - [x] 5.5 — Teste: timeout de 5s nao bloqueia sessao

## Dev Notes
Contexto tecnico relevante:

### Algoritmo de similarity
Duas abordagens possiveis:
- **Keyword matching** (v1): extrair keywords do problema e comparar com patterns dos projetos. Simples, rapido, sem dependencia externa.
- **Embeddings** (v2): usar embedding model (e.g., Anthropic ou OpenAI embeddings) para cosine similarity. Mais preciso, mas adiciona dependencia e custo.

Recomendacao: comecar com keyword matching (v1) e migrar para embeddings quando tiver dados reais para validar.

### Referencia da arquitetura
A secao 8 do `teamAI-Master-Architecture.md` define o contrato:
```python
class CrossProjectContextProvider:
    def find_relevant_past_decision(
        self, current_project, current_problem, user_id
    ) -> Optional[CrossProjectInsight]

    def format_insight(self, insight: CrossProjectInsight) -> str
```

### Tabela Supabase
Usa `user_projects` existente: `(user_id, project_name, claude_md, patterns)`.
Story 6.2 garante que essa tabela esteja populada.

### Testing
- Similarity: problema similar detectado com confidence > 0.7
- False positive: problema diferente nao notifica
- Spam prevention: mesmo insight nao notifica 2x
- Timeout: execucao nao bloqueia sessao principal
- Threshold: valor customizado funciona corretamente

## File List
- `squads-base/mmos-squad/scripts/cross_project_context.py` — CrossProjectContextProvider com keyword similarity, spam prevention, format_insight
- `squads-base/mmos-squad/tests/test_cross_project_context.py` — 9 testes cobrindo todos os ACs

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-26 | 1.0 | Story criada | Morgan (PM) |
| 2026-02-26 | 1.1 | Tasks 1-5 implementadas (exceto 1.2 Supabase load — deferred) | Dev Agent |
| 2026-02-27 | 1.2 | Task 1.2 implementada: load_user_projects() com fallback Supabase | Dev Agent |
