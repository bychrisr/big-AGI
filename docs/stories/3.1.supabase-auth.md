# Story 3.1: Configurar Supabase Auth no big-AGI fork

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [auth_flow_validation, middleware_coverage, session_persistence_test]

## Story
**As a** usuario do teamAI,
**I want** autenticar via Supabase Auth com email/password,
**so that** minha sessao seja segura, persistida e isolada de outros usuarios.

## Acceptance Criteria
1. Supabase Auth SDK (`@supabase/supabase-js`, `@supabase/ssr`) instalado
2. Middleware de autenticacao em todas as rotas protegidas (API + pages)
3. Login/logout funcional com email/password
4. Sessao persistida via cookie (nao localStorage)
5. Redirect para /login se nao autenticado

## Tasks / Subtasks
- [ ] Task 1: Instalar e configurar Supabase SDK (AC: 1)
  - [ ] 1.1 Instalar `@supabase/supabase-js` e `@supabase/ssr` via npm
  - [ ] 1.2 Adicionar variaveis de ambiente `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY` em `src/server/env.server.ts` (server) e env client schema
  - [ ] 1.3 Criar client helpers: `src/common/supabase/client.ts` (browser client) e `src/common/supabase/server.ts` (server client com cookies)
  - [ ] 1.4 Adicionar variaveis ao `.env.example`

- [ ] Task 2: Criar middleware de autenticacao (AC: 2)
  - [ ] 2.1 Criar `middleware.ts` na raiz do projeto (Next.js middleware)
  - [ ] 2.2 Middleware valida sessao Supabase em todas as rotas exceto `/login`, `/api/health`, `/_next/`, `/favicon.ico`
  - [ ] 2.3 Middleware renova token se expirado (refresh token flow)
  - [ ] 2.4 Retorna 401 para rotas API sem sessao valida
  - [ ] 2.5 Redirect para `/login` para rotas de pagina sem sessao valida

- [ ] Task 3: Criar pagina de login (AC: 3)
  - [ ] 3.1 Criar `pages/login.tsx` com formulario email/password usando Material-UI Joy
  - [ ] 3.2 Implementar `signInWithPassword` via Supabase Auth
  - [ ] 3.3 Tratar erros: credenciais invalidas, rede, rate limit
  - [ ] 3.4 Redirect para `/` apos login bem-sucedido
  - [ ] 3.5 Implementar botao de logout no header/menu existente (Optima layout)

- [ ] Task 4: Persistir sessao via cookies (AC: 4)
  - [ ] 4.1 Usar `@supabase/ssr` para gerenciar sessao via cookies (nao localStorage)
  - [ ] 4.2 Configurar cookie options: httpOnly, secure (prod), sameSite strict
  - [ ] 4.3 Testar que sessao sobrevive refresh da pagina
  - [ ] 4.4 Testar que sessao expira corretamente

- [ ] Task 5: Remover HTTP Basic Auth (AC: 2, 5)
  - [ ] 5.1 Identificar todos os usos de `HTTP_BASIC_AUTH_USERNAME` e `HTTP_BASIC_AUTH_PASSWORD` em `src/server/env.server.ts` (linhas 113-114)
  - [ ] 5.2 Buscar referencias em todo o codebase (`rg "HTTP_BASIC_AUTH"`)
  - [ ] 5.3 Remover logica de HTTP Basic Auth, substituir por verificacao Supabase JWT
  - [ ] 5.4 Remover variaveis de env do schema Zod em `env.server.ts`
  - [ ] 5.5 Atualizar documentacao (`docs/deploy-authentication.md`)

- [ ] Task 6: Proteger rotas de API existentes (AC: 2)
  - [ ] 6.1 Criar helper `getAuthenticatedUser(req)` que extrai user do JWT Supabase
  - [ ] 6.2 Aplicar em rotas tRPC: adicionar user context ao tRPC context (`src/server/trpc/`)
  - [ ] 6.3 Verificar que rotas edge (`trpc.router-edge.ts`) e cloud (`trpc.router-cloud.ts`) usam auth
  - [ ] 6.4 Testar que chamadas sem auth retornam 401

## Dev Notes

### Arquivos relacionados no big-AGI
- `src/server/env.server.ts` — HTTP Basic Auth vars (linhas 113-114): `HTTP_BASIC_AUTH_USERNAME`, `HTTP_BASIC_AUTH_PASSWORD`
- `src/server/trpc/` — tRPC routers (edge e cloud)
- `src/common/layout/optima/` — layout system (para botao logout)
- `pages/` — Next.js Pages Router (rotas de pagina)
- `app/api/` — Next.js App Router (rotas API)

### Padroes existentes a seguir
- Env vars: usar `createEnv()` de `src/modules/3rdparty/t3-env` com schema Zod
- Layout: componentes usam Material-UI Joy (`@mui/joy`)
- State: Zustand stores com `useShallow` para selectors multi-propriedade
- Auth context: expor user autenticado via React context ou Zustand store

### APIs e tipos relevantes
- `@supabase/ssr`: `createServerClient()`, `createBrowserClient()`
- `@supabase/supabase-js`: `Session`, `User`, `AuthError`
- Next.js middleware: `NextRequest`, `NextResponse`, `middleware.ts` na raiz

### Referencias de codigo existente
- Deploy auth docs: `docs/deploy-authentication.md`
- Middleware Next.js: nenhum `middleware.ts` existe atualmente — sera criado do zero
- tRPC context: `src/server/trpc/` — verificar como context e passado aos routers

### Testing
- Framework: vitest (ou testes manuais para fluxos de auth)
- Localizacao dos testes: colocados ao lado do arquivo
- O que testar: fluxo login/logout, middleware redirect, session persistence, JWT validation
- Testar que rotas protegidas retornam 401 sem auth
- Testar que sessao sobrevive refresh

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-26 | 1.0 | Story criada | Morgan (PM) |
