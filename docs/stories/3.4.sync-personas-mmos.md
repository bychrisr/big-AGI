# Story 3.4: Sync automatico de Personas MMOS para big-AGI

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [sync_correctness, persona_format_validation, startup_test]

## Story
**As a** usuario do teamAI,
**I want** que as personas do big-AGI sejam sincronizadas automaticamente com os minds do MMOS,
**so that** eu veja os minds disponiveis como personas sem configuracao manual.

## Acceptance Criteria
1. Script de sync le `minds/*/metadata.yaml` do repo teamai
2. Gera personas no formato big-AGI (`SimplePersona`: id, name, systemPrompt)
3. Sync executado no build/startup
4. Personas mostram nome e specialty do mind

## Tasks / Subtasks
- [ ] Task 1: Criar modulo de sync de personas (AC: 1, 2)
  - [ ] 1.1 Criar `src/modules/personas-sync/sync-minds-to-personas.ts`
  - [ ] 1.2 Funcao `syncMindsToPersonas(repoPath: string): SimplePersona[]`
  - [ ] 1.3 Le todos os `minds/*/metadata.yaml` do path `${repoPath}/squads-base/mmos-squad/minds/`
  - [ ] 1.4 Para cada mind: le `metadata.yaml` (nome, specialty, tags) + `system_prompt.md` (system prompt)
  - [ ] 1.5 Converte para `SimplePersona`: `{ id: mind.id, name: mind.name, systemPrompt: conteudo_system_prompt, creationDate: 'sync', inputText: mind.specialty, inputProvenance: { type: 'text' } }`
  - [ ] 1.6 Retorna array de `SimplePersona`

- [ ] Task 2: Integrar sync no startup da aplicacao (AC: 3)
  - [ ] 2.1 Opcao A (recomendada): Criar API route `app/api/personas/sync/route.ts` que executa sync e retorna personas
  - [ ] 2.2 Opcao B: Executar sync como script pre-build em `package.json` (`"prebuild": "ts-node scripts/sync-personas.ts"`)
  - [ ] 2.3 Para Opcao A: frontend chama `/api/personas/sync` no startup e popula `store-app-personas`
  - [ ] 2.4 Para Opcao B: gera arquivo JSON estatico `public/personas-mmos.json` que frontend carrega
  - [ ] 2.5 Env var `TEAMAI_REPO_PATH` define onde o repo teamai esta montado (reutilizar da Story 3.3)

- [ ] Task 3: Adaptar store de personas para aceitar sync (AC: 2, 4)
  - [ ] 3.1 Adicionar funcao `importSyncedPersonas(personas: SimplePersona[])` ao `store-app-personas.ts`
  - [ ] 3.2 Funcao marca personas importadas com flag `source: 'mmos-sync'` (adicionar campo opcional)
  - [ ] 3.3 Sync nao apaga personas criadas pelo usuario manualmente
  - [ ] 3.4 Sync atualiza personas existentes com mesmo `id` (upsert)
  - [ ] 3.5 Personas sincronizadas mostram nome e specialty na UI de personas

- [ ] Task 4: Exibir nome e specialty na UI (AC: 4)
  - [ ] 4.1 Verificar como `AppPersonas.tsx` exibe personas atualmente
  - [ ] 4.2 Garantir que `name` do mind aparece como titulo da persona
  - [ ] 4.3 Garantir que `specialty` aparece como descricao/subtitulo
  - [ ] 4.4 Se necessario, adicionar campo `description` a `SimplePersona` para specialty

- [ ] Task 5: Tratamento de erros e fallback (AC: 1, 3)
  - [ ] 5.1 Se `TEAMAI_REPO_PATH` nao configurado: logar warning, nao crashar, personas ficam vazias
  - [ ] 5.2 Se `metadata.yaml` de um mind invalido: pular mind, logar warning, continuar com os outros
  - [ ] 5.3 Se `system_prompt.md` ausente: usar placeholder `"Mind: {name} - {specialty}"`
  - [ ] 5.4 Se sync falhar completamente: manter personas anteriores, logar erro

## Dev Notes

### Arquivos relacionados no big-AGI
- `src/apps/personas/store-app-personas.ts` — store Zustand com `SimplePersona[]`, persistido em localStorage
- `src/apps/personas/AppPersonas.tsx` — componente principal que renderiza lista de personas
- `src/apps/personas/creator/` — componentes de criacao de personas (Creator, FromText, FromYouTube)

### Padroes existentes a seguir
- Store de personas: Zustand `create()` com `persist()` middleware
- SimplePersona tem `MAX_SAVED_PROMPTS = 100` — garantir que sync nao ultrapasse
- Personas sao identificadas por `id` (UUID gerado por `agiUuid('persona-simple')`)
- Para minds sincronizados, usar ID deterministic baseado no mind id: `'mmos-mind-' + mind.id`

### APIs e tipos relevantes
- `SimplePersona` interface: `{ id, name?, systemPrompt, creationDate, pictureUrl?, inputProvenance?, inputText, llmLabel? }`
- `SimplePersonaProvenance`: `{ type: 'youtube', url, title?, thumbnailUrl? } | { type: 'text' }`
- `prependSimplePersona()`: funcao existente para adicionar persona ao store

### Estrutura de um mind no repo teamai
```
squads-base/mmos-squad/minds/paul_graham/
  metadata.yaml        # { id: paul_graham, name: "Paul Graham", specialty: "Startups & Essays", token_budget: 15000, tags: [strategy, startups] }
  system_prompt.md     # System prompt completo do mind
  kb/                  # Knowledge base (nao necessario para sync de personas)
```

### Testing
- Framework: vitest
- Localizacao dos testes: colocados ao lado do arquivo
- O que testar: conversao metadata.yaml para SimplePersona, sync com minds validos/invalidos, upsert logica
- Mock: filesystem reads para `metadata.yaml` e `system_prompt.md`
- Testar que sync preserva personas criadas manualmente

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-26 | 1.0 | Story criada | Morgan (PM) |
